This directory contains a git annex'ed virtual appliance for the class.
Run `git annex get *.ova` to obtain the image from the web.

Whenever you feel you are done with it, just `git annex drop *.ova`.
